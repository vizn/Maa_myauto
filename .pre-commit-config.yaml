repos:
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.6.0
    hooks:
      - id: check-json
        files: "\\.json$"
      - id: pretty-format-json
        args:
          - --autofix
          - --indent
          - "4"
          - --no-sort-keys
          - --no-ensure-ascii
        files: "\\.json$"